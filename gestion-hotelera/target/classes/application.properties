spring.application.name=gestion-hotelera
# MySQL Database Connection
# --- MySQL Database Configuration ---

# URL de conexi�n, aseg�rate que el nombre de la BD (ej. gestion_hotel) es correcto
spring.datasource.url=jdbc:mysql://localhost:3306/gestion_hotel

# Usuario y contrase�a SIN ESPACIOS al final
spring.datasource.username=mysql
spring.datasource.password=root

# --- JPA/Hibernate Configuration ---
# Para que Hibernate cree/actualice las tablas autom�ticamente
spring.jpa.hibernate.ddl-auto=update

# Para ver las consultas SQL en la consola (opcional pero muy �til)
spring.jpa.show-sql=true